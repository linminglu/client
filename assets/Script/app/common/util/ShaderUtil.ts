



export namespace ShaderUtil {
    // export function setGray(node, bGray: boolean = true) {
    //     let program = bGray ? cc.Scale9Sprite.WebGLRenderCmd._getGrayShaderProgram() : cc.shaderCache.programForKey(cc.macro.SHADER_SPRITE_POSITION_TEXTURECOLOR)
    //     let sprite = node.getComponent(cc.Sprite)
    //     let display = node.getComponent(dragonBones.ArmatureDisplay)
    //     if (sprite) {
    //         sprite._sgNode.setState(bGray ? cc.Scale9Sprite.state.GRAY : cc.Scale9Sprite.state.NORMAL)
    //     }

    //     if (display && display._sgNode) {
    //         let children = display._sgNode.getChildren()
    //         for (let j = 0; j < children.length; j++) {
    //             children[j]._renderCmd._shaderProgram = program
    //         }
    //     }
    // }

    // export function setNormal(node, bGray: boolean = false) {
    //     setGray(node, false)
    // }
}